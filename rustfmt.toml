edition = "2021"
max_width = 100
tab_spaces = 4
hard_tabs = false
newline_style = "Unix"
reorder_imports = true
reorder_modules = true
# imports_granularity, group_imports, trailing_comma=Vertical require nightly rustfmt;
# we re-enable them via `cargo +nightly fmt` once the project standardizes on it.
