//! Top-level `apply(currency, item, omens, rng) -> Result<Item, EngineError>`.
//!
//! Stub for M1.
//! Performance target: sub-millisecond per call (the advisor's beam search runs
//! tens of thousands of these during a re-plan).
